These scripts are not yet working and should not be relied upon for production.

buildvm.sh - call this with sudo to build a vm using vmbuilder scripts
external_tools.sh - this should be called with sudo to install third party tools
config.sh - this should be called as the opencast user, not sudo, it sets up matterhorn, builds it, and attempts to run it